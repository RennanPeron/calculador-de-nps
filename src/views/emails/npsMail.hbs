<style>
    .container{
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .value {
        padding: 10px;
        background: #8257e6;
        color: white;
        border-radius: 6px;
        width:15px;
        text-align: center;
        text-decoration: none;
    }

    .level {
        display: flex;
        margin: 15px 10px;
        justify-content: space-between;
        width: 370px;
        color: grey;
    }

    .answers {
        width: 370px;
        display: flex;
        justify-content: space-between;
    }
</style>

<div class="container">
    <label>Olá, <strong>{{variables.name}}</strong>! Tudo bem?</label>
    <h3>{{variables.title}}</h3>
    <br>
    <strong>{{variables.description}}</strong>

    <div class="level">
        <span>Pouco Provável</span>
        <span>Muito Provável</span>
    </div>

    <div class="answers">
        <a href="{{variables.link}}/1?u={{variables.id}}" class="value">1</a>
        <a href="{{variables.link}}/2?u={{variables.id}}" class="value">2</a>
        <a href="{{variables.link}}/3?u={{variables.id}}" class="value">3</a>
        <a href="{{variables.link}}/4?u={{variables.id}}" class="value">4</a>
        <a href="{{variables.link}}/5?u={{variables.id}}" class="value">5</a>
        <a href="{{variables.link}}/6?u={{variables.id}}" class="value">6</a>
        <a href="{{variables.link}}/7?u={{variables.id}}" class="value">7</a>
        <a href="{{variables.link}}/8?u={{variables.id}}" class="value">8</a>
        <a href="{{variables.link}}/9?u={{variables.id}}" class="value">9</a>
        <a href="{{variables.link}}/10?u={{variables.id}}" class="value">10</a>
    </div>
    <br>
    <br>
    <strong>Sua opinião é muito importante para nós.</strong>
    <h3>Equipe | <strong>NLW</strong></h3>
</div>